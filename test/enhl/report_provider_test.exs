defmodule ENHL.ReportProviderTest do
  use ExUnit.Case, async: true

  test "download report id" do
    # {:ok, fetcher} = ENHL.ReportProvider.start_link
    # assert ENHL.ReportProvider.get_report(fetcher, 93, 100) == nil
  end
end
